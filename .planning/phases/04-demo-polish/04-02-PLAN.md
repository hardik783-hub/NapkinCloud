---
phase: 4
plan: 2
name: Demo Rehearsal HUD & Production README
wave: 2
depends_on: [1]
files_modified:
  - src/components/canvas/DemoHelperHud.tsx
  - src/components/canvas/Canvas.tsx
  - README.md
requirements: [R01, R10, R11, R12]
---

# Plan 04-02: Demo Rehearsal HUD & Production README

## Objective
Build a floating Demo Rehearsal HUD (`DemoHelperHud.tsx`) that provides one-click canvas reset, instant live-state jump for quick video takes, and on-screen timing prompts. Create the production-ready project `README.md` with full architecture documentation, WeMakeDevs rubric alignment, and teammate deployment instructions.

## Code Patterns (Model Resilience)
Conventions:
- Floating collapsible widget in canvas: `fixed bottom-6 right-6 z-30`
- Clear actions: Reset Canvas (`onReset`), Quick Live Jump (`onSimulateLive`)
- Documentation: Clean GitHub Flavored Markdown with Mermaid diagram and CLI instructions

## Tasks

<task type="auto">
  <name>Build Demo Rehearsal HUD Widget</name>
  <files>src/components/canvas/DemoHelperHud.tsx, src/components/canvas/Canvas.tsx</files>
  <action>
    1. Create `DemoHelperHud.tsx`:
       - Compact floating badge: `⚡ Demo HUD (Press D to Toggle)`.
       - When expanded:
         - Visual 3-minute video storyboard breakdown.
         - Button: `🔄 Reset Canvas (Clean State)`.
         - Button: `🚀 Quick Jump to Live (Demo Take)`.
    2. Mount in `Canvas.tsx` with `D` keyboard shortcut listener.
  </action>
  <verify>npm run build</verify>
  <done>Demo HUD renders cleanly and provides instant reset and live simulation triggers</done>
</task>

<task type="auto">
  <name>Create Production README & Teammate Hand-off Guide</name>
  <files>README.md</files>
  <action>
    1. Write comprehensive `README.md`:
       - High-concept pitch & problem statement.
       - Architecture diagram (Mermaid).
       - Features overview & tech stack.
       - Quickstart guide (`npm run dev`, `npm run build`, `npm run test:e2e`).
       - Teammate Step Functions / CloudFormation deployment guide.
       - Video storyboard alignment.
  </action>
  <verify>npm run build</verify>
  <done>Production-grade README created at repository root</done>
</task>

## Must-Haves
- Clean floating Demo HUD for video recording
- Complete, polished README with quickstart and architecture
